/**
 * Same card as Open Graph. Twitter and X read their own tag, so the file
 * exists, but there is no reason for it to be a different picture.
 */
export { default, alt, size, contentType } from "./opengraph-image";
